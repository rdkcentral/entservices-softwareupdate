set(autostart ${PLUGIN_NETWORK_AUTOSTART})

if(PLUGIN_NETWORK_STARTUPORDER)
set (startuporder ${PLUGIN_NETWORK_STARTUPORDER})
endif()

map()
   key(root)
   map()
       kv(mode ${PLUGIN_DUMMYPLUGIN_MODE})
       kv(locator lib${PLUGIN_IMPLEMENTATION}.so)
   end()
end()
ans(configuration)

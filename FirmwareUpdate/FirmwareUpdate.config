set(autostart ${PLUGIN_FIRMWAREUPDATE_AUTOSTART})

if(PLUGIN_FIRMWAREUPDATE_STARTUPORDER)
set (startuporder ${PLUGIN_FIRMWAREUPDATE_STARTUPORDER})
endif()

map()
   key(root)
   map()
       kv(mode ${PLUGIN_FIRMWAREUPDATE_MODE})
       kv(locator lib${PLUGIN_IMPLEMENTATION}.so)
   end()
end()
ans(configuration)

